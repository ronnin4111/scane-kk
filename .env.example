# ============================================
# KK Scan - Environment Variables
# ============================================
# Copy this file to .env.local (or .env) and fill in the values.
# NEVER commit .env.local to version control!

# ---- AI API Keys ----

# Google Gemini API (optional - used as fallback if Z.AI fails)
# Get free key at: https://aistudio.google.com/apikey
# Free tier: 15 RPM, 1500 requests/day for gemini-2.0-flash
GEMINI_API_KEY=your-gemini-api-key-here

# ---- Z.AI Configuration (PRIMARY AI Provider) ----
# In sandbox/dev: Z.AI works automatically without configuration.
# In production (Vercel/hosting): you MUST set these.
# Get API key at: https://chat.z.ai → Settings → API Keys
ZAI_BASE_URL=https://api.z.ai/api/v1
ZAI_API_KEY=your-zai-api-key-here

# ---- Database ----
# SQLite (local development)
DATABASE_URL=file:./db/custom.db

# ---- Next.js ----
# NextAuth.js secret (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=your-nextauth-secret-here

# ---- App Settings (Optional) ----
# Maximum file size for upload (in MB, default 10)
# MAX_FILE_SIZE_MB=10

# OCR timeout (in seconds, default 120)
# OCR_TIMEOUT_SECONDS=120
